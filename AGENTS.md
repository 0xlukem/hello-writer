- Canonical policy sources:
  - Content engine rules: `agent-runtime-guidelines.md`
  - Runtime/governance rules: `agent-runtime-guidelines.md`
- Repository-specific constraints:
  - All generated content must be saved to `output/`
  - Content must be in Markdown format
  - Memory files live in `agents/memory/`
  - Agent definitions live in `agents/` (generated by `setup.sh`)
  - Workflow definitions live in `workflows/`
  - Agent templates live in `templates/agents/`
  - User configuration lives in `config/content-engine.yml`
- Required final verification:
  - All sessions must end with feedback-architect and history-logger
  - Output folder must contain `_meta.md` with session metadata
  - Memory updates require engineer approval via orchestrator checkpoint
